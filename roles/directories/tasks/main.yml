---
# roles/directories/tasks/main.yml
# Creates essential directories for the user environment

- name: Ensure essential directories exist
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: "0755"
  loop: "{{ directories }}"
